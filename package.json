{
  "name": "drug-interaction-awareness-system",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack pharmacovigilance-inspired drug interaction and counseling platform.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "install:all": "npm install",
    "seed": "npm run seed --workspace server",
    "build": "npm run build --workspace client",
    "start": "npm --prefix server start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
